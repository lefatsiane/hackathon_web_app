import { router, useLocalSearchParams } from 'expo-router';
import { useEffect, useState } from 'react';
import { ActivityIndicator, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';
import { colors, type } from '@/constants/theme';
import { loadEmployerDashboard, loadStudentDashboard, Opportunity, Student } from '@/lib/api';
import { getProfileId } from '@/lib/storage';

const initials = (name: string, fallback = 'GR') => name.trim().split(/\s+/).slice(0, 2).map((part) => part[0]).join('').toUpperCase() || fallback;
const Stat = ({ icon, label, value, note, tone }: { icon: string; label: string; value: string | number; note: string; tone: string }) => <View style={styles.stat}><View style={[styles.statIcon, { backgroundColor: tone }]}><Text style={styles.statIconText}>{icon}</Text></View><View style={styles.statCopy}><Text style={styles.statLabel}>{label}</Text><Text style={styles.statValue}>{value}</Text><Text style={styles.muted}>{note}</Text></View></View>;
const OpportunityCard = ({ item }: { item: Opportunity }) => <View style={styles.card}><View style={styles.cardRow}><View style={styles.companyLogo}><Text style={styles.companyLogoText}>{initials(item.employers?.company_name || 'Employer')}</Text></View><View style={styles.cardMain}><View style={styles.cardTitleRow}><View style={styles.flex}><Text style={styles.cardTitle}>{item.title}</Text><Text style={styles.muted}>{item.employers?.company_name || 'Employer'} · {item.type || 'Opportunity'}</Text></View><Text style={styles.save}>♡</Text></View><View style={styles.tags}>{(item.required_skills || []).map((skill) => <Text style={styles.tag} key={skill}>{skill}</Text>)}</View><Text style={styles.reason}>{item.reasoning || item.description || 'A new opportunity on GraduRat.'}</Text><View style={styles.cardBottom}><Text style={styles.muted}>{item.type || 'Opportunity'}</Text><Text style={styles.muted}>{item.description?.split('\n').find((line) => line.startsWith('Location:'))?.replace('Location: ', '') || 'Location flexible'}</Text><Text style={styles.match}>{item.match_score === undefined ? 'New' : `${item.match_score}% Match`}</Text></View></View></View></View>;
const CandidateCard = ({ candidate }: { candidate: Student & { match_score?: number; matching_skills?: string[]; reasoning?: string } }) => <View style={styles.card}><View style={styles.cardRow}><View style={styles.candidateAvatar}><Text style={styles.companyLogoText}>{initials(candidate.full_name)}</Text></View><View style={styles.cardMain}><View style={styles.cardTitleRow}><View style={styles.flex}><Text style={styles.cardTitle}>{candidate.full_name || 'Graduate'}</Text><Text style={styles.muted}>{candidate.qualification || 'Graduate profile'}</Text></View><Text style={styles.match}>{candidate.match_score}% Match</Text></View><View style={styles.tags}>{(candidate.skills || []).map((skill) => <Text style={styles.tag} key={skill}>{skill}</Text>)}</View><Text style={styles.reason}>{candidate.reasoning || candidate.matching_skills?.join(', ') || 'Skills developing'}</Text><View style={styles.cardBottom}><Text style={styles.muted}>Open to work</Text><Pressable onPress={() => {}}><Text style={styles.link}>View Profile →</Text></Pressable></View></View></View></View>;

export default function DashboardScreen() {
  const { role = 'student' } = useLocalSearchParams<{ role?: string }>();
  const isStudent = role !== 'employer';
  const [name, setName] = useState('');
  const [opportunities, setOpportunities] = useState<Opportunity[]>([]);
  const [candidates, setCandidates] = useState<(Student & { match_score?: number; matching_skills?: string[]; reasoning?: string })[]>([]);
  const [stats, setStats] = useState({ first: 0, second: 0 });
  const [state, setState] = useState<'loading' | 'error' | 'content'>('loading');
  const [message, setMessage] = useState('Loading your dashboard...');
  useEffect(() => { let active = true; (async () => { try { const id = await getProfileId(isStudent ? 'student' : 'employer'); if (!id) throw new Error('Register a profile before opening the dashboard.'); if (isStudent) { const result = await loadStudentDashboard(id); if (active) { setName(result.student.full_name); setOpportunities(result.opportunities); setStats({ first: result.stats.matches, second: result.stats.opportunities }); } } else { const result = await loadEmployerDashboard(id); if (active) { setName(result.employer.company_name); setOpportunities(result.opportunities); setCandidates(result.candidates as typeof candidates); setStats({ first: result.stats.active_jobs, second: result.stats.candidates }); } } if (active) setState('content'); } catch (error) { if (active) { setState('error'); setMessage(error instanceof Error ? error.message : 'Could not load dashboard.'); } } })(); return () => { active = false; }; }, [isStudent]);
  if (state === 'loading') 
    return <View style={styles.center}><ActivityIndicator color={colors.blue} /><Text style={styles.muted}>{message}</Text></View>;
  return <View style={styles.safe}><View style={styles.sidebar}><Text style={styles.logo}><Text style={styles.blue}>G</Text>radu<Text style={styles.blue}>R</Text>at</Text><View style={styles.nav}><Text style={[styles.navItem, styles.navActive]}>⌂  Dashboard</Text><Text style={styles.navItem}>{isStudent ? '✦  AI Matches' : '▣  Post a Job'}</Text><Text style={styles.navItem}>♡  Saved {isStudent ? 'Jobs' : 'Candidates'}</Text><Text style={styles.navItem}>◫  Applications</Text><Text style={styles.navItem}>◉  Messages</Text></View><View style={styles.nav}><Text style={styles.navItem}>⚙  Settings</Text><Pressable onPress={() => router.replace('/')}><Text style={styles.navItem}>↪  Log Out</Text></Pressable></View></View><ScrollView contentContainerStyle={styles.main} contentInsetAdjustmentBehavior="automatic"><View style={styles.topbar}><TextInput style={styles.search} placeholder={isStudent ? 'Search jobs, companies or skills...' : 'Search candidates, skills or jobs...'} placeholderTextColor={colors.subtle} /><View style={styles.user}><View style={styles.smallAvatar}><Text style={styles.companyLogoText}>{initials(name)}</Text></View><View><Text style={styles.userName}>{name || (isStudent ? 'Graduate' : 'Employer')}</Text><Text style={styles.muted}>{isStudent ? 'Job Seeker' : 'Employer'}</Text></View></View></View><View style={styles.content}><View style={styles.welcome}><View style={styles.flex}><Text style={styles.smallTitle}>{isStudent ? 'GRADURAT AI' : 'EMPLOYER PORTAL'}</Text><Text style={styles.heading}>{isStudent ? <>Welcome back, <Text style={styles.blue}> {name || 'Graduate'}.</Text></> : <>Find your next <Text style={styles.blue}>great hire.</Text></>}</Text><Text style={styles.muted}>{isStudent ? "We've found new opportunities that match your skills and career preferences." : 'GraduRat has found graduates whose skills match your organisation needs.'}</Text></View>{!isStudent && <Pressable style={styles.primary} onPress={() => router.push('/post-job')}><Text style={styles.primaryText}>+ Post a Job</Text></Pressable>}</View><View style={styles.stats}><Stat icon="✦" label={isStudent ? 'AI MATCHES' : 'ACTIVE JOBS'} value={stats.first} note={isStudent ? '+6 this week' : 'Current opportunities'} tone={colors.blueDark} /><Stat icon={isStudent ? '♡' : '✦'} label={isStudent ? 'SAVED JOBS' : 'AI CANDIDATE MATCHES'} value={isStudent ? 0 : stats.second} note={isStudent ? 'Saved locally' : '50%+ match'} tone="#352060" /><Stat icon="✓" label="APPLICATIONS" value={0} note="Coming soon" tone="#064c3d" /><Stat icon="★" label={isStudent ? 'PROFILE SCORE' : 'SAVED CANDIDATES'} value={isStudent ? '33%' : 0} note={isStudent ? 'Complete your profile' : 'Saved locally'} tone="#633c12" /></View>{state === 'error' ? <View style={styles.empty}><Text style={styles.error}>{message}</Text><Pressable style={styles.primary} onPress={() => router.replace(`/dashboard?role=${isStudent ? 'student' : 'employer'}`)}><Text style={styles.primaryText}>Try again</Text></Pressable></View> : <View style={styles.list}><View style={styles.sectionHeading}><View><Text style={styles.smallTitle}>{isStudent ? 'AI POWERED' : 'GRADURAT AI'}</Text><Text style={styles.sectionTitle}>{isStudent ? 'Recommended for you' : 'Recommended candidates'}</Text></View><Text style={styles.link}>View all →</Text></View>{isStudent ? opportunities.map((item) => <OpportunityCard key={item.id} item={item} />) : candidates.filter((candidate) => (candidate.match_score || 0) >= 50).map((candidate) => <CandidateCard key={candidate.id} candidate={candidate} />)}{isStudent && opportunities.length === 0 && <Text style={styles.muted}>No matching opportunities yet. Check back soon.</Text>}{!isStudent && candidates.length === 0 && <Text style={styles.muted}>No candidate matches yet. Publish a job to start matching.</Text>}</View>}</View></ScrollView></View>;
}

const styles = StyleSheet.create({ safe: { flex: 1, flexDirection: 'row', backgroundColor: colors.background }, sidebar: { width: 180, padding: 24, backgroundColor: '#030811', borderRightColor: '#101e34', borderRightWidth: 1, justifyContent: 'space-between' }, logo: { color: colors.text, fontSize: 24, fontWeight: '800', marginBottom: 36 }, blue: { color: colors.blue }, nav: { gap: 8 }, navItem: { color: colors.subtle, paddingVertical: 12, fontSize: 13 }, navActive: { color: colors.text, backgroundColor: colors.blueDark, borderRadius: 7, paddingHorizontal: 9 }, main: { flexGrow: 1 }, topbar: { minHeight: 74, padding: 17, borderBottomColor: '#0d192c', borderBottomWidth: 1, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', gap: 16 }, search: { flex: 1, maxWidth: 390, paddingHorizontal: 14, paddingVertical: 11, borderColor: colors.border, borderWidth: 1, borderRadius: 8, color: colors.text, backgroundColor: colors.surface, fontSize: 12 }, user: { flexDirection: 'row', alignItems: 'center', gap: 9 }, smallAvatar: { width: 36, height: 36, borderRadius: 18, alignItems: 'center', justifyContent: 'center', backgroundColor: colors.blue }, companyLogoText: { color: colors.text, fontWeight: '800', fontSize: 12 }, userName: { color: colors.text, fontWeight: '700' }, content: { padding: 28, gap: 25 }, welcome: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', gap: 18 }, flex: { flex: 1 }, smallTitle: { color: '#7daeff', fontSize: 11, fontWeight: '800', letterSpacing: 1.2 }, heading: { ...type.title, color: colors.text, marginVertical: 8 }, muted: { color: colors.muted, fontSize: 13, lineHeight: 20 }, primary: { alignSelf: 'flex-start', backgroundColor: '#1265e9', borderRadius: 8, paddingHorizontal: 16, paddingVertical: 13 }, primaryText: { color: colors.text, fontWeight: '800' }, stats: { flexDirection: 'row', flexWrap: 'wrap', gap: 12 }, stat: { flex: 1, minWidth: 145, padding: 15, borderColor: colors.border, borderWidth: 1, borderRadius: 10, backgroundColor: colors.surface, flexDirection: 'row', gap: 11 }, statIcon: { width: 34, height: 34, borderRadius: 8, alignItems: 'center', justifyContent: 'center' }, statIconText: { color: colors.text, fontSize: 17 }, statCopy: { gap: 2 }, statLabel: { color: colors.subtle, fontSize: 10, fontWeight: '800' }, statValue: { color: colors.text, fontSize: 24, fontWeight: '800' }, list: { gap: 14 }, sectionHeading: { flexDirection: 'row', alignItems: 'flex-end', justifyContent: 'space-between' }, sectionTitle: { color: colors.text, ...type.section, marginTop: 4 }, link: { color: '#7daeff', fontWeight: '700', fontSize: 12 }, card: { padding: 17, borderColor: colors.border, borderWidth: 1, borderRadius: 10, backgroundColor: colors.surface, gap: 12 }, cardRow: { flexDirection: 'row', gap: 12 }, companyLogo: { width: 42, height: 42, borderRadius: 9, alignItems: 'center', justifyContent: 'center', backgroundColor: colors.blueDark }, candidateAvatar: { width: 42, height: 42, borderRadius: 21, alignItems: 'center', justifyContent: 'center', backgroundColor: '#352060' }, cardMain: { flex: 1, gap: 10 }, cardTitleRow: { flexDirection: 'row', gap: 10 }, cardTitle: { color: colors.text, fontSize: 16, fontWeight: '800' }, save: { color: colors.subtle, fontSize: 22 }, tags: { flexDirection: 'row', flexWrap: 'wrap', gap: 6 }, tag: { color: '#9ac7ff', backgroundColor: colors.blueDark, borderRadius: 5, paddingHorizontal: 8, paddingVertical: 5, fontSize: 11 }, reason: { color: colors.muted, fontSize: 12, lineHeight: 18 }, cardBottom: { flexDirection: 'row', justifyContent: 'space-between', gap: 8 }, match: { color: colors.green, fontWeight: '800', fontSize: 12 }, empty: { padding: 24, gap: 15, borderColor: colors.border, borderWidth: 1, borderRadius: 10 }, error: { color: colors.danger, lineHeight: 20 }, center: { flex: 1, alignItems: 'center', justifyContent: 'center', gap: 12, backgroundColor: colors.background } });
